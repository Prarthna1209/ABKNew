export interface WorksheetItems
{
  id: string,
  unit: string,
  quantity: number,
  listPrice: number,
  net: number,
  worksheetId: string,
  manufecturerId: string,
  productId: string,
  createdAt: string,
  updatedAt: string
}
